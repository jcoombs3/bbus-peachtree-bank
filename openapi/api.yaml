openapi: 3.0.1
info:
  title: Sample Pockets API
  description: >-
    Optional multiline or single-line description in
    [CommonMark](http://commonmark.org/help/) or HTML.
  version: 0.1.9
servers:
  - url: 'http://localhost:4201/api'
    description: 'Optional server description, e.g. Main (production) server'
paths:
  /pockets:
    get:
      summary: Returns a list of pockets.
      description: Optional extended description in CommonMark or HTML.
      responses:
        '200':
          description: A JSON array of pockets
          content:
            application/json:
              schema:
                type: array
                items: 
                  $ref: '#/components/schemas/Pocket'
  /pockets/{id}:
    get:
      summary: Returns a pocket by ID.
      parameters:
        - name: id
          in: path
          required: true
          description: Parameter description in CommonMark or HTML.
          schema:
            type : string
      responses:
        '200':
          description: A pocker object
          content:
            application/json:
              example:
                $ref: 'examples/body/pockets-detail.json'
              schema:
                $ref: '#/components/schemas/Pocket'
components:
  schemas:
    Pocket:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        balance:
          type: string
        goalBalance:
          type: string
        goalDate:
          type: string
        transactions:
          $ref: '#/components/schemas/PocketTransactions'
      required:
        - id
        - name
    PocketTransactions:
      $ref: schemas/body/pocket-transactions.json